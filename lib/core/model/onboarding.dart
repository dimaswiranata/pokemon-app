class OnBoarding {
  String? image;
  String? title;
  String? subtitle;

  OnBoarding({this.image, this.title, this.subtitle});
}

class NavigatorItem {
  int? index;
  String? activeIcon, icon, label;

  NavigatorItem({this.index, this.activeIcon, this.icon, this.label});
}
